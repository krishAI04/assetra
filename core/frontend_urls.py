from django.urls import path

from .web_views import (
    ApprovalsView,
    ApproveDistributionView,
    AssetCreateView,
    DistributionCreateView,
    AssetraLoginView,
    AssetraLogoutView,
    AssetraSignupView,
    AssetUpdateView,
    AuditLogPageView,
    BeneficiaryCreateView,
    BeneficiaryUpdateView,
    ClientCreateView,
    ClientDetailView,
    ClientListView,
    ClientUpdateView,
    DashboardView,
    DocumentCommentCreateView,
    DocumentAIAnalyzeView,
    DocumentPreviewView,
    DocumentVersionCreateView,
    DocumentsView,
    GlobalSearchView,
    HomeRedirectView,
    MarkNotificationsReadView,
    ProtectedDocumentView,
    RejectDistributionView,
    TaskListView,
    TaskStatusView,
    TaskUpdateView,
    UserManagementView,
    ClientCommentCreateView,
)

urlpatterns = [
    path("", HomeRedirectView.as_view(), name="home"),
    path("login/", AssetraLoginView.as_view(), name="login"),
    path("signup/", AssetraSignupView.as_view(), name="signup"),
    path("logout/", AssetraLogoutView.as_view(), name="logout"),
    path("dashboard/", DashboardView.as_view(), name="dashboard"),
    path("search/", GlobalSearchView.as_view(), name="global-search"),
    path("notifications/read/", MarkNotificationsReadView.as_view(), name="notifications-read"),
    path("clients/", ClientListView.as_view(), name="clients"),
    path("clients/new/", ClientCreateView.as_view(), name="client-create"),
    path("clients/<int:pk>/", ClientDetailView.as_view(), name="client-workspace"),
    path("clients/<int:pk>/edit/", ClientUpdateView.as_view(), name="client-edit"),
    path("clients/<int:pk>/comments/", ClientCommentCreateView.as_view(), name="client-comment-create"),
    path("assets/new/", AssetCreateView.as_view(), name="asset-create"),
    path("assets/<int:pk>/edit/", AssetUpdateView.as_view(), name="asset-edit"),
    path("distributions/new/", DistributionCreateView.as_view(), name="distribution-create"),
    path("beneficiaries/new/", BeneficiaryCreateView.as_view(), name="beneficiary-create"),
    path("beneficiaries/<int:pk>/edit/", BeneficiaryUpdateView.as_view(), name="beneficiary-edit"),
    path("documents/", DocumentsView.as_view(), name="documents"),
    path("documents/<int:pk>/preview/", DocumentPreviewView.as_view(), name="document-preview"),
    path("documents/<int:pk>/analyze/", DocumentAIAnalyzeView.as_view(), name="document-ai-analyze"),
    path("documents/<int:pk>/open/", ProtectedDocumentView.as_view(), name="document-open"),
    path("documents/<int:pk>/comments/", DocumentCommentCreateView.as_view(), name="document-comment-create"),
    path("documents/<int:pk>/versions/", DocumentVersionCreateView.as_view(), name="document-version-create"),
    path("tasks/", TaskListView.as_view(), name="tasks"),
    path("tasks/<int:pk>/edit/", TaskUpdateView.as_view(), name="task-edit"),
    path("tasks/<int:pk>/status/", TaskStatusView.as_view(), name="task-status"),
    path("approvals/", ApprovalsView.as_view(), name="approvals"),
    path(
        "approvals/<int:pk>/approve/",
        ApproveDistributionView.as_view(),
        name="distribution-approve",
    ),
    path(
        "approvals/<int:pk>/reject/",
        RejectDistributionView.as_view(),
        name="distribution-reject",
    ),
    path("users/", UserManagementView.as_view(), name="users"),
    path("audit-log/", AuditLogPageView.as_view(), name="audit-log"),
]
